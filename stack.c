#include "monty.h"
/**
 * f_stack - this function prints the top
 * @head: this points to the stack head
 * @counter: this is the line_number
 * Return: the function has no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
